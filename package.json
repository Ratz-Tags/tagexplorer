{
  "name": "kexplorer",
  "version": "1.0.0",
  "description": "The **Kink Artist Explorer** is a mobile-first, degrading tool for discovering NSFW artists on Danbooru based on humiliating kink tags like femdom, chastity, and sissy training.",
  "main": "main.js",
  "scripts": {
    "test": "node --test",
    "build:scripts": "tsc -p tsconfig.scripts.json",
    "update:tags": "npm run build:scripts && node scripts/updateKinkTags.js",
    "update:audio": "npm run build:scripts && node scripts/generate-audio-list.js",
    "build:css": "tailwindcss -i styles/tailwind.css -o style.css --minify",
    "serve": "npx http-server -c-1 . -p 8080 --silent"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "@types/node": "^22.8.1",
    "tailwindcss": "^3.4.13",
    "typescript": "^5.4.5",
    "undici-types": "^6.19.8"
  }
}
